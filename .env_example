# Django Configuration
# Generate a secure secret key using:
# python manage.py shell -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY=your-django-secret-key-here

# Debug mode: Set to 1 for development, 0 for production
DEBUG=1

# Allowed hosts (comma-separated, or leave empty for all hosts)
# For production, specify your domain: example.com,www.example.com
ALLOWED_HOSTS=

# CSRF Trusted Origins (required for Cloud Run)
# After deployment, get your Cloud Run URL and set it here, e.g.:
# CSRF_TRUSTED_ORIGINS=https://webglazer-prototype-xxxxx-uc.a.run.app
# Or set it in Cloud Run environment variables
CSRF_TRUSTED_ORIGINS=

# OpenAI API Configuration
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your-openai-api-key-here

